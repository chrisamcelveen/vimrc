platform :ios, '9.0'
use_frameworks!

def available_pods

	pod 'SideMenuController'
	
end

liftsync_targets = ['LiftSync Coach', 'LiftSync Athlete']

liftsync_targets.each do |liftsync_target|
 target liftsync_target do
   available_pods
 end
end
